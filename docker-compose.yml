version: "3.9"

services:
  backend:
    build: .
    container_name: 3plfast-backend
    restart: unless-stopped
    ports:
      - "5001:5001"
    environment:
      NODE_ENV: production
      PORT: 5001

      SUPABASE_URL: "https://taboklgtcpykicqufkha.supabase.co"
      SUPABASE_ANON_KEY: "REPLACE_WITH_SUPABASE_ANON_KEY"
      SUPABASE_SERVICE_ROLE_KEY: "REPLACE_WITH_SUPABASE_SERVICE_ROLE_KEY"

      CLIENT_URL: "https://lemon-smoke-0bf242700.2.azurestaticapps.net"
      BACKEND_URL: "https://max-2-pay-backend.vercel.app"

      ENABLE_INVOICE_CRON: "false"
      INTERNAL_SERVICE_KEY: "3plfast-internal-service-key-change-in-production-2026"

      CLOUDINARY_CLOUD_NAME: "REPLACE_WITH_CLOUDINARY_CLOUD_NAME"
      CLOUDINARY_API_KEY: "REPLACE_WITH_CLOUDINARY_API_KEY"
      CLOUDINARY_API_SECRET: "REPLACE_WITH_CLOUDINARY_API_SECRET"
